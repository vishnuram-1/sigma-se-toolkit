"""Per-SE configuration. Edit REPS to filter calls to your reps."""

# Comma-separated rep names (no space after the comma).
# Empty string = no rep filter; the export returns everything the workbook
# would return without any control applied.
#
# Example: REPS = "Jane Doe,John Smith,Maria Garcia"
REPS = ""
