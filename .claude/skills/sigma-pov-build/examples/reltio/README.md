# Reltio anchor example

Placeholder. Will be filled in after the first end-to-end run on Reltio with:

- `scoping.md` (redacted) — what the enriched scoping doc looks like in practice
- `data-models/gtm_interactions.json` — the deployed data model spec
- `workbooks/reltio_gtm.json` — the deployed workbook spec

Use this directory as a diff target when building for future prospects — the structure and conventions should match.
